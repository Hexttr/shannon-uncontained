# 📊 Анализ сервера для запуска пентеста через Ollama

**Дата анализа:** 2025-01-XX  
**Сервер:** 72.56.79.153  
**Статус:** Частично готов

---

## ✅ ЧТО УЖЕ ГОТОВО

### Система
- ✅ **OS:** Ubuntu Linux (6.8.0-90-generic)
- ✅ **RAM:** 15GB (доступно 14GB)
- ✅ **Disk:** 154GB (использовано 22%, свободно 121GB)

### Node.js окружение
- ✅ **Node.js:** v20.20.0 (требуется 18+)
- ✅ **npm:** 10.8.2
- ✅ **Проект:** `/root/shannon-uncontained` существует
- ✅ **Зависимости:** `node_modules` установлен
- ✅ **Критичные файлы:** `shannon.mjs`, `local-source-generator.mjs`, `src/ai/llm-client.js` присутствуют

### Ollama (LLM провайдер)
- ✅ **Ollama:** Установлен (`/usr/local/bin/ollama`)
- ✅ **Сервис:** Работает (порт 11434)
- ✅ **Модель:** `codellama:7b` (3.8 GB) загружена
- ✅ **Конфигурация:** `.env` настроен:
  ```
  LLM_PROVIDER=ollama
  LLM_MODEL=codellama:7b
  ```

### Инструменты пентестинга (частично)
- ✅ **nmap:** v7.94SVN (требуется для `NetReconAgent`)
- ✅ **whatweb:** v0.5.5 (требуется для `TechFingerprinterAgent`)
- ✅ **python3:** v3.12.3

---

## ❌ ЧТО НУЖНО УСТАНОВИТЬ

### Критично (REQUIRED)

#### 1. Go (язык программирования)
**Статус:** ❌ Не установлен  
**Критичность:** ВЫСОКАЯ  
**Причина:** Необходим для установки Go-based инструментов (subfinder, katana, nuclei)

**Установка:**
```bash
# На сервере
curl -fsSL https://go.dev/dl/go1.21.5.linux-amd64.tar.gz -o /tmp/go.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf /tmp/go.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
echo 'export GOPATH=$HOME/go' >> ~/.bashrc
echo 'export PATH=$PATH:$GOPATH/bin' >> ~/.bashrc
source ~/.bashrc
go version  # Проверка
```

#### 2. subfinder
**Статус:** ❌ Не установлен  
**Критичность:** ВЫСОКАЯ (REQUIRED)  
**Агент:** `SubdomainHunterAgent`  
**Назначение:** Перечисление поддоменов

**Установка (после установки Go):**
```bash
go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
# Убедитесь что $GOPATH/bin в PATH
subfinder -version  # Проверка
```

### Важно (опционально, но рекомендуется)

#### 3. katana
**Статус:** ❌ Не установлен  
**Критичность:** СРЕДНЯЯ  
**Агент:** `CrawlerAgent`  
**Назначение:** Веб-краулинг и обнаружение эндпоинтов

**Установка:**
```bash
go install github.com/projectdiscovery/katana/cmd/katana@latest
katana -version  # Проверка
```

#### 4. nuclei
**Статус:** ❌ Не установлен  
**Критичность:** СРЕДНЯЯ  
**Агент:** `NucleiScanAgent`  
**Назначение:** Сканирование уязвимостей (CVE, эксплойты)

**Установка:**
```bash
go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest
nuclei -version  # Проверка
# После установки обновить шаблоны:
nuclei -update-templates
```

#### 5. httpx
**Статус:** Не проверен (вероятно отсутствует)  
**Критичность:** СРЕДНЯЯ  
**Назначение:** HTTP probing и валидация эндпоинтов

**Установка:**
```bash
go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
httpx -version  # Проверка
```

#### 6. gau (GetAllUrls)
**Статус:** Не проверен (вероятно отсутствует)  
**Критичность:** НИЗКАЯ  
**Агент:** `CrawlerAgent`  
**Назначение:** Сбор URL из архивов (Wayback Machine, CommonCrawl)

**Установка:**
```bash
go install github.com/lc/gau/v2/cmd/gau@latest
gau --version  # Проверка
```

---

## 📋 ПЛАН ДЕЙСТВИЙ

### Шаг 1: Установка Go
```bash
ssh root@72.56.79.153
curl -fsSL https://go.dev/dl/go1.21.5.linux-amd64.tar.gz -o /tmp/go.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf /tmp/go.tar.gz
echo 'export PATH=$PATH:/usr/local/go/bin:$HOME/go/bin' >> ~/.bashrc
echo 'export GOPATH=$HOME/go' >> ~/.bashrc
source ~/.bashrc
go version
```

### Шаг 2: Установка критичных инструментов
```bash
# subfinder (REQUIRED)
go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest

# Проверка установки
subfinder -version
```

### Шаг 3: Установка рекомендуемых инструментов
```bash
# katana (краулинг)
go install github.com/projectdiscovery/katana/cmd/katana@latest

# nuclei (сканирование уязвимостей)
go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest
nuclei -update-templates

# httpx (HTTP probing)
go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest

# gau (URL harvesting)
go install github.com/lc/gau/v2/cmd/gau@latest

# Проверка всех инструментов
which subfinder katana nuclei httpx gau
```

### Шаг 4: Проверка конфигурации проекта
```bash
cd /root/shannon-uncontained

# Проверить .env
cat .env | grep -E 'LLM_PROVIDER|LLM_MODEL'
# Должно быть:
# LLM_PROVIDER=ollama
# LLM_MODEL=codellama:7b

# Проверить доступность Ollama
curl http://localhost:11434/api/tags

# Проверить модель
ollama list
```

### Шаг 5: Тестовый запуск
```bash
cd /root/shannon-uncontained

# Проверка доступности инструментов через preflight
node -e "
import('./src/local-source-generator/v2/tools/preflight.js').then(m => {
  m.runPreflight({requiredOnly: true}).then(r => {
    console.log('Required tools:', r.hasRequired ? '✅ All' : '❌ Missing');
    r.missing.forEach(t => console.log('  Missing:', t.name));
  });
});
"

# Простой тест запуска (если все инструменты установлены)
./shannon.mjs generate https://example.com --workspace ./test-output
```

---

## 🔍 ДОПОЛНИТЕЛЬНЫЕ ПРОВЕРКИ

### Проверка портов
- ✅ **11434:** Ollama работает (localhost)
- ✅ **3000:** Node.js приложение работает
- ✅ **8080:** Другое Node.js приложение

### Проверка модели Ollama
Текущая модель: `codellama:7b` (3.8 GB)

**Рекомендации:**
- Для базового пентеста: `codellama:7b` достаточно
- Для более качественного анализа можно использовать:
  ```bash
  ollama pull llama3.2        # Быстрее, меньше RAM
  ollama pull llama3.1:70b     # Качественнее, требует больше RAM
  ```

**Обновление .env для другой модели:**
```bash
# В /root/shannon-uncontained/.env
LLM_MODEL=llama3.2  # или llama3.1:70b
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. PATH для Go инструментов
После установки Go инструментов убедитесь, что `$HOME/go/bin` в PATH:
```bash
echo $PATH | grep -q go/bin || export PATH=$PATH:$HOME/go/bin
```

### 2. Права доступа
Убедитесь что все инструменты исполняемые:
```bash
chmod +x $HOME/go/bin/*
```

### 3. Ollama модель
Текущая модель `codellama:7b` специализирована для кода, что хорошо для пентестинга. Если нужен более общий анализ, рассмотрите `llama3.2`.

### 4. Опциональные инструменты
Следующие инструменты опциональны, но расширяют функциональность:
- **feroxbuster** / **ffuf** - Content discovery (для `ContentDiscoveryAgent`)
- **trufflehog** / **gitleaks** - Secret scanning (для `SecretScannerAgent`)
- **wafw00f** - WAF detection (для `WAFDetector`)
- **sslyze** - TLS анализ (для `TLSAnalyzer`)
- **sqlmap** - SQL injection (для `SQLmapAgent`)
- **xsstrike** - XSS валидация (для `XSSValidatorAgent`)
- **commix** - Command injection (для `CommandInjectionAgent`)

---

## 📊 СТАТУС ГОТОВНОСТИ

| Компонент | Статус | Приоритет |
|-----------|--------|-----------|
| Система | ✅ Готово | - |
| Node.js | ✅ Готово | - |
| Ollama | ✅ Готово | - |
| Проект | ✅ Готово | - |
| **Go** | ❌ **Нужна установка** | **КРИТИЧНО** |
| **subfinder** | ❌ **Нужна установка** | **КРИТИЧНО** |
| nmap | ✅ Готово | - |
| whatweb | ✅ Готово | - |
| katana | ❌ Рекомендуется | Средний |
| nuclei | ❌ Рекомендуется | Средний |
| httpx | ❓ Не проверен | Средний |
| gau | ❓ Не проверен | Низкий |

---

## 🚀 БЫСТРЫЙ СТАРТ (после установки Go)

```bash
# 1. Подключиться к серверу
ssh root@72.56.79.153

# 2. Установить Go (см. Шаг 1 выше)

# 3. Установить критичные инструменты
go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
export PATH=$PATH:$HOME/go/bin

# 4. Проверить установку
subfinder -version

# 5. Запустить пентест
cd /root/shannon-uncontained
./shannon.mjs generate https://example.com --workspace ./test-output
```

---

## 📝 ЗАМЕТКИ

1. **Минимальная конфигурация:** Для базового пентеста достаточно установить Go и subfinder. Остальные инструменты опциональны.

2. **Полная конфигурация:** Для использования всех возможностей LSGv2 рекомендуется установить все перечисленные инструменты.

3. **Модель Ollama:** `codellama:7b` подходит для пентестинга. Если есть больше RAM (16GB+), можно использовать `llama3.1:70b` для лучшего качества анализа.

4. **Производительность:** Сервер имеет достаточно ресурсов (15GB RAM, 121GB свободного места) для работы с Ollama и всеми инструментами.

---

**Следующий шаг:** Установить Go и критичные инструменты, затем выполнить тестовый запуск пентеста.

