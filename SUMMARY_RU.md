# 📋 Резюме: Что нужно сделать для запуска пентеста через Ollama

## ✅ Текущее состояние сервера

### Готово к работе:
- ✅ **Система:** Ubuntu Linux, 15GB RAM, достаточно места на диске
- ✅ **Node.js:** v20.20.0 установлен и работает
- ✅ **Ollama:** Установлен, запущен, модель `codellama:7b` загружена
- ✅ **Проект:** Клонирован в `/root/shannon-uncontained`, зависимости установлены
- ✅ **Конфигурация:** `.env` настроен для работы с Ollama
- ✅ **Инструменты:** nmap, whatweb, python3 установлены

### Требует установки:
- ❌ **Go** (язык программирования) - КРИТИЧНО
- ❌ **subfinder** (перечисление поддоменов) - КРИТИЧНО (REQUIRED)
- ❌ **katana** (веб-краулинг) - Рекомендуется
- ❌ **nuclei** (сканирование уязвимостей) - Рекомендуется
- ❌ **httpx** (HTTP probing) - Рекомендуется
- ❌ **gau** (сбор URL) - Опционально

---

## 🚀 Быстрый старт

### Вариант 1: Автоматическая установка (рекомендуется)

Используйте скрипт `setup_server_tools.py`:

```bash
python setup_server_tools.py
```

Скрипт автоматически:
1. Установит Go
2. Установит все критичные инструменты (subfinder)
3. Установит рекомендуемые инструменты (katana, nuclei, httpx, gau)
4. Обновит шаблоны nuclei
5. Проверит установку

### Вариант 2: Ручная установка

Подключитесь к серверу и выполните:

```bash
ssh root@72.56.79.153

# 1. Установка Go
curl -fsSL https://go.dev/dl/go1.21.5.linux-amd64.tar.gz -o /tmp/go.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf /tmp/go.tar.gz
rm /tmp/go.tar.gz
mkdir -p $HOME/go/bin
echo 'export PATH=$PATH:/usr/local/go/bin:$HOME/go/bin' >> ~/.bashrc
echo 'export GOPATH=$HOME/go' >> ~/.bashrc
source ~/.bashrc

# 2. Установка критичных инструментов
go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest

# 3. Установка рекомендуемых инструментов
go install github.com/projectdiscovery/katana/cmd/katana@latest
go install -v github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest
go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
go install github.com/lc/gau/v2/cmd/gau@latest

# 4. Обновление шаблонов nuclei
nuclei -update-templates

# 5. Проверка установки
which subfinder katana nuclei httpx gau
subfinder -version
```

---

## ✅ Проверка готовности

После установки проверьте:

```bash
# 1. Проверка Go
go version
# Должно показать: go version go1.21.5 linux/amd64

# 2. Проверка инструментов
subfinder -version
katana -version
nuclei -version
httpx -version
gau --version

# 3. Проверка Ollama
curl http://localhost:11434/api/tags
ollama list

# 4. Проверка проекта
cd /root/shannon-uncontained
cat .env | grep LLM_PROVIDER
# Должно быть: LLM_PROVIDER=ollama
```

---

## 🧪 Тестовый запуск

После установки всех инструментов:

```bash
cd /root/shannon-uncontained

# Простой тест
./shannon.mjs generate https://example.com --workspace ./test-output

# Или с дополнительными опциями
./shannon.mjs run https://example.com --workspace ./test-output --profile recon-only
```

---

## 📊 Минимальная конфигурация

Для базового пентеста достаточно установить:
1. ✅ Go
2. ✅ subfinder (REQUIRED)

Остальные инструменты опциональны, но расширяют функциональность:
- **katana** - для краулинга и обнаружения эндпоинтов
- **nuclei** - для сканирования уязвимостей
- **httpx** - для валидации HTTP эндпоинтов
- **gau** - для сбора исторических URL

---

## 📝 Важные заметки

1. **PATH:** После установки Go переподключитесь к серверу или выполните `source ~/.bashrc` для применения изменений PATH.

2. **Модель Ollama:** Текущая модель `codellama:7b` подходит для пентестинга. Если нужно лучшее качество анализа и есть больше RAM, можно использовать `llama3.1:70b`:
   ```bash
   ollama pull llama3.1:70b
   # Затем обновить .env: LLM_MODEL=llama3.1:70b
   ```

3. **Права доступа:** Убедитесь что все инструменты исполняемые:
   ```bash
   chmod +x $HOME/go/bin/*
   ```

4. **Обновление шаблонов:** После установки nuclei рекомендуется обновить шаблоны:
   ```bash
   nuclei -update-templates
   ```

---

## 🔍 Дополнительная информация

Подробный анализ сервера см. в файле `SERVER_ANALYSIS_RU.md`

Скрипт автоматической установки: `setup_server_tools.py`

Анализ текущего состояния: `analyze_server.py`

---

## ❓ Решение проблем

### Проблема: Инструменты не найдены после установки
**Решение:** Проверьте PATH:
```bash
echo $PATH | grep go/bin
# Если пусто, добавьте:
export PATH=$PATH:$HOME/go/bin
source ~/.bashrc
```

### Проблема: Ollama не отвечает
**Решение:** Перезапустите Ollama:
```bash
pkill ollama
ollama serve &
```

### Проблема: Модель не найдена
**Решение:** Проверьте доступные модели:
```bash
ollama list
# Если нужной модели нет, загрузите:
ollama pull codellama:7b
```

---

**Статус:** Готово к установке недостающих компонентов  
**Приоритет:** Установить Go и subfinder для базового функционала

